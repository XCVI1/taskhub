apiVersion: v1
kind: Service
metadata:
  name: {{ include "tasks-service.fullname" . }}
  labels:
    {{- include "tasks-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8002
      protocol: TCP
      name: http
  selector:
    {{- include "tasks-service.selectorLabels" . | nindent 4 }}
